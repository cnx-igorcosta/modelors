/*const mongoose = require('mongoose');
const ModeloRs = require('modeloRs');
const Schema = mongoose.Schema;

const tagSchema = new Schema({
  nome: {type : String},
  modelosRs: [ {type : mongoose.Schema.ObjectId, ref : 'ModeloRs'} ]
});

const TagRs = mongoose.model('Tag', tagSchema);

module.exports = TagRs;*/
